services:
  - type: web
    name: TranslationGrizlikBot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: BOT_TOKEN
        value: YOUR_BOT_TOKEN_HERE
